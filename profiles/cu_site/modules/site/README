Use this directory to store the site custom modules, each in its own directory. The contributed modules and the shared Creighton modules will be added to the site profile directory when running the make file.
